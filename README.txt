This demo implements the Courselets chat UI using client-side Python async code (via Brython).  This is a convenient way to rapid-prototype changes to our chat UI.

To run the demo, start the built-in Python HTTP server by

    python3 -m http.server


Then load http://localhost:8000/faqdemo.html in the browser address bar.

